Add the finished ebook PDFs in this folder.

Then import them in `src/data/ebookFiles.js`:

```js
import websiteDesignRo from './ebooks/website-design-ro.pdf'
import websiteDesignEn from './ebooks/website-design-en.pdf'
```

Use the imported variables in the matching `ro` and `en` slots.
